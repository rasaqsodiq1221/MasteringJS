'use strict'

/* function testingCodeRunner(app) {
    if (app === 'coderunner') {
        return `the ${app} runs perfectly `
    }else {
        return "something is still working"
    }
}

console.log(testingCodeRunner("codeRunner")) */

function welcomeToVscode() {
    console.log(helloWorld())
}

function helloWorld() {
    return "hello world"
}

welcomeToVscode();